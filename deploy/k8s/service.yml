apiVersion: v1
kind: Service
metadata:
  name: go-rest-api
  namespace: go-rest
spec:
  selector:
    app: go-rest-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP